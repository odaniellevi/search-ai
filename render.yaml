services:
  - type: web
    name: search-ai
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn backend:app --timeout 120 --workers 1
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.7
    plan: free
